import { ResidentDetailClient } from "./resident-detail-client";

export const metadata = {
  title: "Chi tiết cư dân | Habi Admin"
};

export default function ResidentDetailPage() {
  return <ResidentDetailClient />;
}
